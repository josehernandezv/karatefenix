<script lang="ts">
	import type { PageData } from './$types';

	export let data: PageData;
</script>

{#each data.posts as post}
	<a href={`/blog/${post.slug}`}>
		<h2>{post.title}</h2>
	</a>
{/each}
